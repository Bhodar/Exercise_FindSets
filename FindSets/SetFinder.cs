using System;

namespace FindSets
{
    public class SetFinder
    {
        /// <summary>
        /// A valid set is of length <paramref name="setSize"/> and contains cards with unique symbols and no card can be used across multiple sets
        /// </summary>
        /// <param name="cards"></param>
        /// <param name="setSize"></param>
        /// <param name="maxNumberOfValues"></param>
        /// <returns></returns>
        public int[] HighestValuesInValidSets(Card[] cards, int setSize, int maxNumberOfValues)
        {
            return null;
        }

        /// <summary>
        /// A valid set is of length <paramref name="int1"/> and contains cards with unique symbols and no card can be used across multiple sets
        /// </summary>
        /// <param name="cards"></param>
        /// <param name="setSize"></param>
        /// <returns></returns>
        public int NumberOfValidSets(Card[] cards, int setSize)
        {
            return 0;
        }
    }
}
